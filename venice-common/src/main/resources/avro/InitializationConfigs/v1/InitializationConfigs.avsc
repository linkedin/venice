{
  "name": "InitializationConfigs",
  "namespace": "com.linkedin.venice.ingestion.protocol",
  "type": "record",
  "fields": [
    {
      "name": "aggregatedConfigs",
      "doc": "A map of string key and string map value. This map contains a set of config to initialize all components required for ingestion",
      "type": {
        "type": "map",
        "values": "string"
      },
      "default": {}
    }
  ]
}
