apply plugin: 'java'
apply plugin: 'idea'
apply plugin: 'eclipse'

// TODO: Define the dependencies here. Refer to them in the module build files.

sourceCompatibility = 1.6
version = '0.1'

task buildAll {
  dependsOn 'venice-common:buildJar'
  dependsOn 'venice-client:installApp'
  dependsOn 'venice-server:installApp'
  dependsOn 'venice-controller:installApp'
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.10'
}