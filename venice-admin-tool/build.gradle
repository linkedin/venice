apply plugin: 'java'

jar {
    from { configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } }
    manifest {
        attributes 'Main-Class': 'com.linkedin.venice.AdminTool'
    }
}

dependencies {
    compile project(':venice-common')
}
