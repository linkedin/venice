Venice Admin Tool
-----------------

For directly affecting administrative actions and queries on a Venice cluster

```
usage: java -jar venice-admin-tool-0.1.jar --<command> [parameters]
            Commands:
    --add-schema                 Requires: --router, --cluster, --store,
                                 --value-schema-file
    --describe-store             Requires: --router, --cluster, --store
    --describe-stores            Requires: --router, --cluster
    --job-status                 Query the ingest status of a running push
                                 job.  Requires: --router, --cluster,
                                 --store, --version
    --list-storage-nodes         Requires: --router, --cluster
    --list-stores                Requires: --router, --cluster
    --new-store                  Requires: --router, --cluster, --store,
                                 --version, --key-schema-file,
                                 --value-schema-file
    --node-removable             A node is removable if all replicas it is
                                 serving are available on other nodes.
                                 Requires: --router, --cluster,
                                 --storage-node
    --pause-store                Prevent a store from accepting new
                                 versions.  Requires: --router, --cluster,
                                 --store
    --query                      Query a store that has a simple key
                                 schema.  Requires: --router, --cluster,
                                 --store, --key
    --replicas-of-store          List the location and status of all
                                 replicas for a store.  Requires:
                                 --router, --cluster, --store, --version
    --replicas-on-storage-node   List the store and status of all replicas
                                 on a storage node.  Requires: --router,
                                 --cluster, --storage-node
    --resume-store               Allow a store to accept new versions
                                 again after being paused.  Requires:
                                 --router, --cluster, --store
    --set-version                Set the version that will be served.
                                 Requires: --router, --cluster, --store,
                                 --version
usage: Parameters:
 -c,--cluster <arg>              Name of Venice cluster
 -f,--filter-json <arg>          Comma-delimited list of fields to display
                                 from the json output.  Omit to display
                                 all fields
 -fj,--flat-json                 Display output as flat json, without
                                 pretty-print indentation and line breaks
 -h,--help                       Show usage
 -k,--key <arg>                  Plain-text key for identifying a record
                                 in a store
 -ks,--key-schema-file <arg>     Path to text file with key schema
 -n,--storage-node <arg>         Helix instance ID for a storage node, eg.
                                 lva1-app1234_1690
 -o,--owner <arg>                Owner email for new store creation
 -r,--router <arg>               Venice router url, eg.
                                 http://localhost:1689
 -s,--store <arg>                Name of Venice store
 -v,--version <arg>              Venice store version number
 -vs,--value-schema-file <arg>   Path to text file with value schema

Examples:
java -jar venice-admin-tool-0.1.jar --list-stores --router <router> --cluster <cluster>
java -jar venice-admin-tool-0.1.jar --describe-store --router <router> --cluster <cluster> --store <store>
java -jar venice-admin-tool-0.1.jar --describe-stores --router <router> --cluster <cluster>
java -jar venice-admin-tool-0.1.jar --pause-store --router <router> --cluster <cluster> --store <store>
java -jar venice-admin-tool-0.1.jar --resume-store --router <router> --cluster <cluster> --store <store>
java -jar venice-admin-tool-0.1.jar --job-status --router <router> --cluster <cluster> --store <store> --version <version>
java -jar venice-admin-tool-0.1.jar --new-store --router <router> --cluster <cluster> --store <store> --version <version> --key-schema-file <key-schema-file> --value-schema-file <value-schema-file>
java -jar venice-admin-tool-0.1.jar --set-version --router <router> --cluster <cluster> --store <store> --version <version>
java -jar venice-admin-tool-0.1.jar --add-schema --router <router> --cluster <cluster> --store <store> --value-schema-file <value-schema-file>
java -jar venice-admin-tool-0.1.jar --list-storage-nodes --router <router> --cluster <cluster>
java -jar venice-admin-tool-0.1.jar --node-removable --router <router> --cluster <cluster> --storage-node <storage-node>
java -jar venice-admin-tool-0.1.jar --replicas-of-store --router <router> --cluster <cluster> --store <store> --version <version>
java -jar venice-admin-tool-0.1.jar --replicas-on-storage-node --router <router> --cluster <cluster> --storage-node <storage-node>
java -jar venice-admin-tool-0.1.jar --query --router <router> --cluster <cluster> --store <store> --key <key>

```
