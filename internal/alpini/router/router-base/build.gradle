dependencies {
  compile project(':internal:alpini:common:common-io')
  compile project(':internal:alpini:router:router-api')
  compileOnly project(':internal:alpini:netty4:netty4-base')
  compileOnly libraries.mail
  testCompile libraries.testng
  testCompile project(':internal:alpini:netty4:netty4-base')
  testCompile libraries.mail
  implementation libraries.javaxActivation
}
